# Gemfile for Jekyll + Minimal Mistakes (remote theme)
# Run: bundle install && bundle exec jekyll serve

source "https://rubygems.org"

gem "jekyll"
gem "github-pages", group: :jekyll_plugins
gem "jekyll-paginate-v2", group: :jekyll_plugins
